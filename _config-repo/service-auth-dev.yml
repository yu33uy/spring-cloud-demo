server:
  context-path: /uaa
  port: 5000

spring:
  datasource:
    url: jdbc:mysql://mysql:3306/spring-cloud-demo
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    database: MYSQL
    generate-ddl: false
    show-sql: true
    hibernate:
      ddl-auto: none

management:
  security:
    enabled: false

security:
  oauth2:
    clientId: clientid
    secret: secret