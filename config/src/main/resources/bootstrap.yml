spring:
  cloud:
    config:
      server:
        git:
          uri: "git@github.com:zhxiaowu/spring-cloud-demo.git"
          search-paths: _config-repo
server:
  port: 8888

security:
  user:
    password: config-password
    name: config-user
